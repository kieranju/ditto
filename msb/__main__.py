from .operator.definitions import MButton, KKey
from .simulated import m_move, m_press, m_release, m_click, m_hit, k_press, k_release, k_click, k_hit, k_type

# m_move(50, 50)
# m_press(MButton.RIGHT)
# m_move(65, 86)
# m_release(MButton.RIGHT)
# k_hit(KKey.A)

# k_type('hello, this is a phrase')
